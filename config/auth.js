// Webhook Authentication Configuration
export const webhookAuth = {
  username: 'username',  // Change this to your preferred username
  password: 'password'   // Change this to your preferred password
};

// You can also use environment variables for better security
// export const webhookAuth = {
//   username: process.env.WEBHOOK_USERNAME || 'username',
//   password: process.env.WEBHOOK_PASSWORD || 'password'
// };
